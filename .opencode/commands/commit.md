---
description: Hacer commit con mensaje descriptivo
agent: build
---

Review the current changes:
!`git diff --stat`

Create a meaningful commit message and commit all changes.
Use conventional commits format.
